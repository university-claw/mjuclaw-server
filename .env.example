# NemoClaw 샌드박스 (모델: gemini-3.1-flash-lite-preview, nemoclaw onboard 단계에서 설정)
GEMINI_API_KEY=
SANDBOX_NAME=mjuclaw

# Server
PORT=3000
SERVER_URL=https://your-domain.com

# Security — 32바이트 키를 hex로 (생성: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=
ADMIN_KAKAO_ID=
